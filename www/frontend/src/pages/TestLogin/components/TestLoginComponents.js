import React, { Component } from 'react';
import { Button } from 'antd';

class TestLoginComponents extends Component {

  render() {
    return (
      <div>
        <Button>Default</Button>
      </div>
    );
  }
}
export default (TestLoginComponents);
